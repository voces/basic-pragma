name: tests
on:
- push
- workflow_dispatch
jobs:
  setup:
    steps:
    - uses: actions/checkout@v3
  test:
    needs: setup
    steps:
    - uses: actions/setup-node@v3
      with:
        node-version: '18.x'
    - run: npm install
    - run: npm run build
    - run: npm test
  lint:
    needs: setup
    steps:
    - uses: denoland/setup-deno@v1
    - run: deno lint . --ignore=node_modules,dist,src/test/jsIntegration/dist